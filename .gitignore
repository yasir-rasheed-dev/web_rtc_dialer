node_modules/
dist/
.DS_Store
npm-debug.log*
*.zip

# Large raw DB dump — not source, kept locally only
before-saas-db.sql

# marketing site — large binary, distributed via the server not git
website/downloads/*.exe
